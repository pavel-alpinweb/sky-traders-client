import * as Phaser from "phaser"

export class TownScene extends Phaser.Scene {
    private name!: string

    constructor(name: string) {
        super()
        this.name = name
    }

    preload() {}

    create() {}

    update() {}
}
